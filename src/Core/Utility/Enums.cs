namespace CoApp.VisualStudio
{
    public enum DeveloperPackageType
    {
        None, VcInclude, VcLibrary, Net
    }

    public enum FilterType
    {
        None, Highest, Stable, Compatible, Any, X86, X64, Application, Assembly, DeveloperLibrary
    }
}
